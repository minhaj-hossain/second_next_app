
const PostDetails = async ({ params }) => {

    const { postId } = params;

    // you can fetch and  show the data without having any touble

    return (
        <div>
            this is a single page post
        </div>
    );
};

export default PostDetails;